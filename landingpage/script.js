const startDetectingBtn = document.getElementById('startDetecting');

startDetectingBtn.addEventListener('click', () => {
  // Handle the button click event
  // You can implement the image upload and disease detection logic here.
  console.log("Start Detecting button clicked!");
});